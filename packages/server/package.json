{
  "name": "server",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "docker-compose up -d",
    "down": "docker-compose down",
    "log": "docker-compose logs -f",
    "install": "poetry install",
    "add": "poetry add",
    "remove": "poetry remove",
    "export": "rm -f requirements.txt && docker-compose exec app poetry export -f requirements.txt > requirements.txt"
  }
}
